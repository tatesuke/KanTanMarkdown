#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../datauri/bin/datauri" "$@"
  ret=$?
else 
  node  "$basedir/../datauri/bin/datauri" "$@"
  ret=$?
fi
exit $ret
